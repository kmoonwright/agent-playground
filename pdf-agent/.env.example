# Arize AX (required for traces, datasets, experiments, annotations)
ARIZE_API_KEY=
ARIZE_SPACE_ID=
ARIZE_PROJECT_NAME=pdf-extraction-demo

# OpenAI (optional — the pipeline stubs the LLM call if this is unset,
# keeping the same span shape so the demo still works without a key)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
